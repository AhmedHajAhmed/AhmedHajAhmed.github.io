# hajahmed.com

Personal website. Plain HTML and CSS, no dependencies. Deployed via GitHub Pages.

- `index.html` — English
- `zh/index.html` — Chinese
- `styles.css` — styles

Preview locally by opening `index.html`, or run `python -m http.server`.